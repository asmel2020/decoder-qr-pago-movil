// Prefijos de DNI admitidos (una sola letra).
export const ALLOWED_DNI_PREFIXES = new Set<string>(["V", "J", "G", "E"]);

// Prefijo por defecto que se antepone si no se especifica.
export const DEFAULT_DNI_PREFIX = "V";
